[package]
name = "fuzzy-match"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
description = "Greedy fuzzy scoring and stable ranking without application policy"
license = "MIT"
publish = false

[lints]
workspace = true

[dev-dependencies]
allocation-counter = "0.8.1"
