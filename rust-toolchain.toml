# An exact version, not `stable`: the point of pinning is that a formatting
# or lint difference between two machines is a real finding rather than a
# version gap, and `stable` rolls forward underneath you. Raising this is a
# deliberate commit — expect new clippy lints to fire when you do.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
